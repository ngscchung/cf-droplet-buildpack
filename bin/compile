#!/bin/bash

#shopt -s dotglob

echo 'PWD: ' $PWD
ls -l 

echo '/home/vcap/app'

ls -l /home/vcap/app

echo '$1: ' $1

ls -l $1

echo '$2: ' $2

ls -l $2

cd $1

tar xvf *.tar

rm -f *.tar

mv * /home/vcap/app

#mv $1/app/* $1
